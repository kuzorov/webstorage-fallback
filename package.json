{
  "name": "@kuzorov/webstorage-fallback",
  "version": "1.0.1",
  "description": "Polyfill for webstorage even in private tabs, etc",
  "main": "webstorageFallback.min.js",
  "scripts": {
    "build": "uglifyjs --compress --mangle -o webstorageFallback.min.js -- webstorageFallback.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://kuzorov@github.com/kuzorov/local-storage-fallback.git"
  },
  "keywords": [
    "storage",
    "localStorage",
    "sessionStorage",
    "polyfill"
  ],
  "author": "Evgeniy Kuzorov <kuzorov@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/kuzorov/webstorage-fallback/issues"
  },
  "homepage": "https://github.com/kuzorov/webstorage-fallback#readme",
  "devDependencies": {
    "uglify-js": "^3.3.7"
  }
}
